apiVersion: v1
kind: Service
metadata:
  name: gatus-service
  labels:
    app: gatus
spec:
  type: LoadBalancer
  selector:
    app: gatus
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
